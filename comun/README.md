# `comun/` — el módulo compartido

Todavía vacío. Acá va lo que hoy está copiado en cada script: la ventana de
selección de rutas, la lectura y escritura del `config.json`, el log con progreso,
la apertura y cierre de Excel, y el descarte de las copias de Windows
(`- copia`, `(2)`, `~$`).

**Se migra por partes, nunca de golpe.** Una pieza a la vez, un script a la vez,
verificando que sigue corriendo antes de seguir con el próximo. Primera pieza
sugerida: el manejo del `config.json` (documento de dominio, sección 9).

Ver `MAPA.md` → "Lo que todavía no existe".
