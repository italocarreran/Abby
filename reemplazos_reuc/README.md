# `reemplazos_reuc/`

`ActualizaRemplazos.py` y su propio `config.json` en `Auxiliares/` — aparte del
compartido por el revisor y los actualizadores.

Ver el bloque en `MAPA.md`, en particular la corrección del orden: los reemplazos
forzados se aplican **antes** del cruce con el registro de empresas.
